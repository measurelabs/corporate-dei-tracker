# Existing API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: Sanity API Token (required if dataset is private or for authenticated requests)
# Create at: https://www.sanity.io/manage -> Your Project -> API -> Tokens
# Use a token with "Viewer" permissions for read-only access
SANITY_API_TOKEN=
